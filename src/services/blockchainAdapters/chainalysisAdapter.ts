// Placeholder for external vendor integration.
// Keep the interface clean so you can swap vendors.
export async function getSanctionsSignal(_address: string): Promise<number> {
  // Replace with actual API call and normalize to [0,1]
  return 0;
}